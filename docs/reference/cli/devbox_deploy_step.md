## devbox deploy step

Run a single deploy step by <phase>/<step> address

### Synopsis

Execute a single step from the deploy pipeline by its address.

The address format is '<phase>/<step>' (e.g. 'init/render-env') or '<service>/<phase>/<step>'.
Use 'devbox deploy plan' to list available step addresses. Use --dry-run to preview without executing.

```
devbox deploy step <phase>/<step> [flags]
```

### Examples

```
  devbox deploy step init/render-env
  devbox deploy step main/setup/migrate
  devbox deploy step init/render-env --dry-run
```

### Options

```
      --dry-run   print the resolved command without executing
  -h, --help      help for step
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
```

### SEE ALSO

* [devbox deploy](devbox_deploy.md)	 - Deploy pipeline commands

###### Auto generated by spf13/cobra on 15-May-2026
