## devbox deploy state clear

Clear the deploy state

### Synopsis

Delete the deploy state file at .devbox/deploy/state.yml.

This removes all step status records, hashes, and run metrics. The next 'devbox deploy run'
will treat all steps as needing to be executed.

In interactive mode (TTY), a confirmation prompt is shown. Use -y/--non-interactive to skip the prompt.

```
devbox deploy state clear [flags]
```

### Examples

```
  devbox deploy state clear
  devbox deploy state clear -y
```

### Options

```
  -h, --help              help for clear
  -y, --non-interactive   skip confirmation prompt
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
```

### SEE ALSO

* [devbox deploy state](devbox_deploy_state.md)	 - Manage deploy state

###### Auto generated by spf13/cobra on 15-May-2026
