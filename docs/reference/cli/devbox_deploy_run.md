## devbox deploy run

Execute the deploy plan

### Synopsis

Execute the full deploy pipeline from devbox/deploy.yml phase by phase.

Steps are run in declaration order. The .env file is regenerated as the implicit
first step. Use --service to run only the steps relevant to a specific service.

File logging is enabled by default for deploy and writes to logs/deploy.log.
Disable it with 'log: false' at the top of devbox/deploy.yml.

```
devbox deploy run [flags]
```

### Examples

```
  devbox deploy run
  devbox deploy run --service main
```

### Options

```
  -h, --help             help for run
      --service string   deploy a single service only
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
```

### SEE ALSO

* [devbox deploy](devbox_deploy.md)	 - Deploy pipeline commands

###### Auto generated by spf13/cobra on 4-May-2026
