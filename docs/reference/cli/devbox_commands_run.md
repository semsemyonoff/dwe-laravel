## devbox commands run

Run a devbox command

### Synopsis

Execute a declarative command by its dot-separated ID.

Use --set key=value to override declared params at runtime.
Private commands cannot be run directly.

```
devbox commands run <id> [flags]
```

### Examples

```
  devbox commands run db.up
  devbox commands run services.main.migrate --set db=mydb
```

### Options

```
  -h, --help              help for run
      --set stringArray   Set a param value (key=value)
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default "devbox.yml")
```

### SEE ALSO

* [devbox commands](devbox_commands.md)	 - List, inspect, and run devbox commands

###### Auto generated by spf13/cobra on 16-Apr-2026
