## devbox stop

Stop the project (full lifecycle: before-stop hooks → docker down → after-stop hooks)

### Synopsis

Stop the project driven by devbox/lifecycle.yml.

Execution order: before-stop hooks → docker down → after-stop hooks → final message.

Use 'devbox docker down' for a bare Docker Compose stop-and-remove without hooks.
Use 'devbox docker stop' for the low-level compose stop (no container removal).

```
devbox stop [flags]
```

### Examples

```
  devbox stop
```

### Options

```
  -h, --help   help for stop
  -y, --yes    skip confirmation prompts inside hook steps
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
```

### SEE ALSO

* [devbox](devbox.md)	 - devbox-cli — local development environment toolkit

###### Auto generated by spf13/cobra on 15-May-2026
