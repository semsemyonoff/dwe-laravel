## devbox deploy plan

Show resolved deploy plan

### Synopsis

Print all phases and steps from devbox/deploy.yml as they would be executed.

The implicit .env generation step is always shown first. Use --service to filter
the plan to steps relevant to a specific service. Use --format yaml for machine-readable output.

```
devbox deploy plan [flags]
```

### Examples

```
  devbox deploy plan
  devbox deploy plan --service main
  devbox deploy plan --format yaml
```

### Options

```
      --format string    output format: table or shell (default "table")
  -h, --help             help for plan
      --service string   show plan for a single service only
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default "devbox.yml")
```

### SEE ALSO

* [devbox deploy](devbox_deploy.md)	 - Deploy pipeline commands

###### Auto generated by spf13/cobra on 16-Apr-2026
