## devbox render env

Generate .env from exports.env spec (stdout or --output <file>)

### Synopsis

Evaluate the exports.env rules from the merged config and write the resulting .env content.

Rules are declared in devbox/defaults.yml under 'exports.env'. Each rule maps a config
dot-path to an environment variable name with optional format and conditional logic.

Output goes to stdout by default; use --output to write directly to a file.

```
devbox render env [flags]
```

### Examples

```
  devbox render env
  devbox render env -o .env
```

### Options

```
  -h, --help            help for env
  -o, --output string   write to file instead of stdout
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default "devbox.yml")
```

### SEE ALSO

* [devbox render](devbox_render.md)	 - Render derived artifacts from the merged devbox config

###### Auto generated by spf13/cobra on 29-Apr-2026
