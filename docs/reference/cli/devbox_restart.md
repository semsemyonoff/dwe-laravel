## devbox restart

Restart the project (stop, then run --no-update)

### Synopsis

Restart the project by running the full stop lifecycle then the full run lifecycle.

The run leg always skips the git update probe (equivalent to 'devbox run --no-update').

Use 'devbox docker restart' for the low-level compose restart passthrough.

```
devbox restart [flags]
```

### Examples

```
  devbox restart
```

### Options

```
  -h, --help   help for restart
  -y, --yes    skip confirmation prompts inside hook steps
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default: auto-discover from cwd upward)
```

### SEE ALSO

* [devbox](devbox.md)	 - devbox-cli — local development environment toolkit

###### Auto generated by spf13/cobra on 12-May-2026
