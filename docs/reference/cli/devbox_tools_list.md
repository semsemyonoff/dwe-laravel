## devbox tools list

Toggle tools interactively (TTY) or show status table (non-TTY)

### Synopsis

Toggle optional tools on or off using an interactive multi-select form.

On submit, newly-checked tools are enabled and newly-unchecked tools are
disabled in devbox/local.yml; .env is regenerated once.

In non-TTY mode (piped stdin or no terminal) the command falls back to printing
the read-only status table.

```
devbox tools list [flags]
```

### Examples

```
  devbox tools list
  devbox tools list | cat   # non-TTY: prints the table
```

### Options

```
  -h, --help   help for list
```

### Options inherited from parent commands

```
  -c, --config string   path to devbox.yml (default "devbox.yml")
```

### SEE ALSO

* [devbox tools](devbox_tools.md)	 - Manage optional tools

###### Auto generated by spf13/cobra on 29-Apr-2026
