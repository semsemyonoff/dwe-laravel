# services.main.composer-install

Install PHP dependencies via Composer

## Properties

| Property | Value |
|---|---|
| **ID** | `services.main.composer-install` |
| **Type** | `service_exec` |
| **Group** | `services.main` |

**Service:** `app-main`

## Argv

```
composer install --ansi --prefer-dist --no-interaction --optimize-autoloader
```

