# services.second.composer-install

Install PHP dependencies via Composer

## Properties

| Property | Value |
|---|---|
| **ID** | `services.second.composer-install` |
| **Type** | `service_exec` |
| **Group** | `services.second` |

**Service:** `app-second`

## Argv

```
composer install --ansi --prefer-dist --no-interaction --optimize-autoloader
```

