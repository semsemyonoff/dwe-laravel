group:
  title: App
  description: Application-level commands

commands:
  install:
    type: devbox
    description: Install the Laravel application via installer container
    run: "compose raw --bare -- --progress tty -f compose/installer.yml run --rm --quiet-pull -u ${host.uid}:${host.gid} app-install"
